## 后端逻辑增强

1. **模型重构**:

   * 修改 `model/domain_asset.go`，增加 `Children` 字段支持树形 JSON 输出。

   * 完善 `model/certificate_entity.go`，确保包含 `Fingerprint`、`SANs`、`IsWildcard` 等字段。
2. **修复编译错误**:

   * 修正 `service/domain_asset_service.go` 中 `Count` 方法的 `int64` 类型问题。

   * 在 `service/cert_certificate.go` 中内联关联逻辑，彻底消除循环依赖。
3. **功能实现**:

   * 在 `service/subdomain_discovery.go` 中实现 `DiscoverSubdomainsViaCTLog`。

   * 在 `service/cert_probe.go` 中确保 `tls.Config` 正确配置 `ServerName`。

   * 在 `service/cert_certificate.go` 中引入 `sem chan struct{}` 实现并发限制。

## 前端功能实现

1. **树形视图**: 创建 `view/domainTree.vue`，使用 `el-table` 树形模式，实现状态汇总逻辑（子异常则父异常）。
2. **统计看板**: 创建 `view/certStatistics.vue`，集成 ECharts 展示证书覆盖分布。
3. **API 封装**: 在 `api/certAdvanced.js` 中封装子域名发现、树形获取等新接口。

## 部署与验证

1. **环境构建**: 执行 `docker-compose build`。
2. **数据初始化**: 更新 `initialize/menu.go` 和 `initialize/gorm.go`。
3. **功能测试**: 验证根域名添加后的自动发现流程及证书关联准确性。

